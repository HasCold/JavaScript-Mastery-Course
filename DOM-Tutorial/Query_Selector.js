// select element using query selector

// query selector se ham kuch bhi select krskte ha

const header = document.querySelector(".header");
console.log(header);

const navItem = document.querySelectorAll(".nav-items");
console.log(navItem);

